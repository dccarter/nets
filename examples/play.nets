/**
 * Development playgroud for nets
 */

struct Age {
    age: i32
}
enum Tok {
    Add, Sub
}

func main(...args: string[]) {
    const (x,y) : (Tok, Age) = (Tok.Add, Age{age: 32});
    return (x, y);
}
