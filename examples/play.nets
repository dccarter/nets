/**
 * Development playgroud for nets
 */

struct Age {
    age: i32
}
enum Tok {
    Add, Sub
}

pub opaque type println = func(any[]) -> void;

func main(...args: string[]) {
    for (const arg : args) {
        println(arg);
    }
}
